import { createContext } from 'react';

const PageSettingsContext = createContext(null);
const PageSettingsDispatchContext = createContext(null);

export { PageSettingsContext, PageSettingsDispatchContext };